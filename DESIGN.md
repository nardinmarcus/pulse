# DESIGN.md — 脉搏 · Pulse 视觉与交互规范

参照 layout-atlas 的纸墨体系（同一作者同一审美），但按运维工具的场景做了暗色优先的变体。状态页是半夜看的页面，默认跟着系统走，两种主题都必须过硬。

## 1 气质

- 纸墨（paper & ink）：米白纸底 + 近黑墨字；暗色为墨底 + 暖灰字。
- 发丝线（hairline）分区，不用卡片阴影、不用圆角容器堆叠。分组靠 1px 线 + 留白。
- 数字一律 `ui-monospace` 等宽 + `font-variant-numeric: tabular-nums`，毫秒与百分比右对齐。
- 标签小型大写 / 汉字小字号 + 加宽字距，灰阶降一级。
- 唯一的图形修辞：心电线（ECG line）。正常时是一条手绘感心电轨迹，故障时轨迹拉平为直线并转红。除此以外页面没有装饰。

## 2 色彩

| 语义 | Light | Dark |
|---|---|---|
| 纸/墨底 | `#f6f3ec` | `#161514` |
| 墨/字 | `#23201b` | `#e6e1d7` |
| 次级字 | 55% 墨 | 60% 字 |
| 发丝线 | `rgba(35,32,27,.16)` | `rgba(230,225,215,.15)` |
| up / ok | `#337a5b` | `#4ba583` |
| down | `#b0402f` | `#d26a58` |
| paused/new | `#8a8578` | `#928d81` |
| 强调（链接/焦点） | 同 up 色 | 同 up 色 |

状态色只用在小圆点、条带、心电线、关键字上，不做大面积色块。横幅是例外：故障横幅可用极低饱和的红色底（8% 透明度）。

## 3 版式

```
masthead   脉搏 (serif 大字) + PULSE · FLEET STATUS (mono 小型大写)
           右侧: 更新时间 (tabular, 每 1s 走秒)
ecg        心电线 SVG, 100% 宽 × 56px, 1.5px 描边
banner     「全部系统运转正常」/「N 项服务异常」 单行, 上下发丝线
groups     按 Content / Network / Automation / API / Platform 分节
  row      [●] 名称+备注        目标 host(mono, 截断)   uptime90   延迟now   [24h 折线]  [90日条带]
incidents  近 30 日事件列表: 起止时间 · 时长 · 检查名 · 原因
footer     监测说明 · © 2026 Namoo · admin 链接(仅一个小圆点)
```

- 桌面一行放下检查行；≤720px 时折成两行（名称+状态 / 指标行），条带移到第三行占满宽。
- 移动端条带高度 6px，桌面 8px；条宽 2px 间隙 1px，90 根自适应 `flex`。
- 行 hover：整行底色 3% 墨色，不改布局。

## 4 动效

- 心电线：载入时 `stroke-dashoffset` 1.4s 画出（ease-out）。全部正常时轨迹含 QRS 尖峰；故障时换平直线，红色，3s 呼吸透明度。
- down 的状态点：1.6s 缩放脉冲（box-shadow 扩散）。
- `prefers-reduced-motion: reduce`：全部动画关闭，心电线静态。
- 客户端 30s 用 `document.body.outerHTML` 整体替换（渲染函数只在服务端一份，客户端零模板）。替换后重新走一次心电画出动画——这是刻意的：每次刷新都是一次「重新把脉」。

## 5 语言

- 中文为主，英文以小型大写作副标（masthead、banner、节标题皆双语的「汉字+mono 副标」制式）。
- 时间格式：`MM-DD HH:mm`，显式 UTC+8，不打 urn。
- 全部 UI 文案中英对照出现的位置固定：banner、空态、footer 一句。行内标签只中文。

## 6 边界态

- `new`（未首探）：点为空心灰，延迟/在线率显示 `—`。
- `paused`：整行降 45% 不透明度，点为灰，条带全灰。
- 无事件：事件区显示「近 30 日无事件 · no incidents in the last 30 days」+ 一条发丝线空态。
- 样本不足（<24h）：折线照画（短于宽度），uptime 显示「—」并注明「数据积累中」。
